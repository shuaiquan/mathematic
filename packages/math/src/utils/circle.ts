import { Vector2 } from "../unit/vector2";

class CircleUtil {
    static getAngleByPoint(center: Vector2, radius: number, point: Vector2, isClockwise: boolean = false) {
        // todo
    }

    static getPointByAngle(center: Vector2, radius: number, angle: number, isClockwise: boolean = false) {
        // todo
    }
}

export { CircleUtil };
